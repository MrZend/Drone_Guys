import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { AuthSpaceComponent } from './auth-space.component';

@NgModule({
  imports: [
    CommonModule
  ],
  declarations: [AuthSpaceComponent]
})
export class Auth_spaceModule { }
